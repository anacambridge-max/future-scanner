name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      UPSTOX_ACCESS_TOKEN: ci-placeholder
      DATABASE_URL: postgresql://ci:ci@localhost:5432/future_scanner
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - run: npx prisma validate
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
